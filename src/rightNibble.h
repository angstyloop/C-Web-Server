#ifndef RIGHT_NIBBLE_H
#define right_nibble_h

#include "randomBytes.h"
#include "__assert.h"

unsigned char rightNibble(unsigned char x);
void TEST_rightNibble(unsigned char* bytes, size_t numBytes);

#endif
