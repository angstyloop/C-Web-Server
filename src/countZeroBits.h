#ifndef COUNT_ZERO_BITS_H

#include "getBit.h"
#include "countOneBits.h"

#ifdef TEST_COUNT_ZERO_BITS
#include "__assert.h"
#include "returnSuccess.h"
#endif

size_t countZeroBits(size_t size, unsigned char* bytes);

#endif
