#ifndef LEFT_NIBBLE_H
#define LEFT_NIBBLE_H

#include "randomBytes.h"
#include "__assert.h"

unsigned char leftNibble(unsigned char x);
void TEST_leftNibble(unsigned char* bytes, size_t numBytes);

#endif
